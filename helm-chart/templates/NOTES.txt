The public proxy Service is {{ include "pingora-reverse-proxy.fullname" . }}-public.
The management API Service is {{ include "pingora-reverse-proxy.fullname" . }}-api and defaults to ClusterIP.
The metrics Service is {{ include "pingora-reverse-proxy.fullname" . }}-metrics.

Create the referenced authentication and storage Secrets before installing this chart.
The chart never renders secret values.
